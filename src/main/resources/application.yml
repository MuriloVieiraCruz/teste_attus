server:
  port: 8080

spring:
  application:
    name: "testeattus"
  datasource:
    url: jdbc:postgresql://db:5432/attusdb
    username: postgres
    password: post
    pool:
      max-size: 10
      initial-size: 7
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update